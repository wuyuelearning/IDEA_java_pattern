package patterns.state;

/**
 * Created by wuyue on 2018/10/11.
 */
public class describe {
    /**
     *  状态模式 中的行为是由状态决定的，不同状态下有不同的行为。
     *  与策略模式的结构几乎一样，凡是他们的目的和本质是完全不一样的。
     *  状态模式的行为是平行的、不可替换的
     *  策略模式的行为是彼此独立、可以替换的。
     *  即
     *  状态模式把对象的行为包装在不同的状态对象里，每一个对象都有一个共同的抽象状态基类。
     *  状态模式的意图是让一个对象在其内部状态改变的时候，其行为也随之改变
     */
}
